{{!

}}
<div data-toggle="true" class="label label-module label-{{module}} pull-left">{{moduleIconLabel 'KBRevisions'}}</div> <h4 data-toggle="true" class="pull-left">{{str layout.label parentModule}}</h4>
{{#each meta.fields}}
    <span class="pull-left panel-field">
        {{field ../this model=../model}}
    </span>
{{/each}}
<div class="subpanel-controls btn-toolbar pull-right">
    {{#each meta.buttons}}
        {{field ../this}}
    {{/each}}
</div>
